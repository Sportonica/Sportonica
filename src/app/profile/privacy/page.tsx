import type { Metadata } from "next";
import PrivacySection from "./PrivacySection";

export const metadata: Metadata = { title: "Privacy — Sportonica" };

export default function PrivacyPage() {
  return <PrivacySection />;
}
